# Dependencies
node_modules/
.pnp
.pnp.js

# Build
dist/
build/
*.tsbuildinfo

# Environment — NE JAMAIS COMMITER (.env.production EST commité, valeurs publiques Vite)
.env
.env.local
.env.*.local
*.env
.env.prod
infra/prod/.env.prod
infra/prod/bind9/zones/*.jnl
infra/prod/bind9/zones/tsig.key
infra/prod/caddy/data/
infra/prod/caddy/config/
infra/prod/vault-keys.txt
infra/prod/.unseal-keys

# Secrets — NE JAMAIS COMMITER
*.key
*.pem
*.p12
*.pfx
vault-keys.json
unseal-keys.txt

# Prisma
prisma/migrations/*.sql.bak

# Backups locaux (dumps DB pré-migration, non versionnés)
backups/

# Claude Code — allow-list locale + runtime (contient des passwords de test
# en dev ; à NE PAS publier. À nettoyer avant passage en prod — cf.
# docs/SECURITY_CHECKLIST_PROD.md).
.claude/settings.json
.claude/scheduled_tasks.lock

# Logs
logs/
*.log
npm-debug.log*

# Coverage
coverage/
.nyc_output

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker volumes (local)
.docker/

# Test
junit.xml
test-results/

# Security audit reports
Result_Secu_test.md
infra/caddy/certs/
secrets/

# Playwright
test/playwright/.auth/
test/playwright/.report/
test-results/
playwright-report/

# Audit logs (locaux uniquement, contiennent UUIDs / IPs)
audit_2026-*/

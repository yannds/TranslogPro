# Production env — TransLog Pro SaaS sur dsyann.info
# Vite charge ce fichier en priorité sur .env quand mode=production (vite build).
# CHECK : ne committer QUE des valeurs publiques (les VITE_* sont visibles dans le bundle).
VITE_PLATFORM_BASE_DOMAIN=translog.dsyann.info
VITE_API_URL=https://api.translog.dsyann.info
